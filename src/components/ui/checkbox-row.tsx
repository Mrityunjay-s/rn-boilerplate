import { Text, View } from "react-native";

// TODO: implement CheckboxRow component
export function CheckboxRow({ label }: { label: string }) {
  return (
    <View>
      <Text>{label}</Text>
    </View>
  );
}
